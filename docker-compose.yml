version: "3"
services:
  postgres:
    image: postgres:15.3
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: ${PSQL_USER}
      POSTGRES_PASSWORD: ${PSQL_PWD}
      POSTGRES_DB: ${PSQL_DB}
